<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    tools:ignore="SelectedPhotoAccess">
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"
        tools:ignore="ForegroundServicesPolicy" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROCESSING"
        tools:ignore="SelectedPhotoAccess" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"
        tools:ignore="PhotoAndVideoPolicy,SelectedPhotoAccess" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"
        tools:ignore="PhotoAndVideoPolicy,SelectedPhotoAccess" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".VideoEditorApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_app_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VideoEditorApp">
        <service
            android:name=".service.ExportService"
            android:exported="false"
            android:foregroundServiceType="mediaProcessing" />
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/files_paths" />
        </provider>
        <activity
             android:name=".SplashActivity"
             android:exported="true"
             android:theme="@style/Theme.VideoEditorApp.Dark">
             <intent-filter>
                 <action android:name="android.intent.action.MAIN" />
                 <category android:name="android.intent.category.LAUNCHER" />
             </intent-filter>
        </activity>
        <activity android:name=".IntroActivity" android:theme="@style/Theme.VideoEditorApp" android:exported="false"/>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.VideoEditorApp" />
        <activity
            android:name=".ExportActivity" android:theme="@style/Theme.VideoEditorApp"
            android:launchMode="singleTop" />


        <activity android:name=".MyProjectsActivity"
            android:label="My Projects"
            android:theme="@style/Theme.VideoEditorApp"/>

        <activity android:name=".TutorialActivity"  android:theme="@style/Theme.VideoEditorApp"/>
        <activity android:name=".TutorialDetailActivity"  android:theme="@style/Theme.VideoEditorApp" android:label="Tutorial Detail"/>
        <activity android:name=".HelpActivity"  android:theme="@style/Theme.VideoEditorApp"/>
        <activity android:name=".FavoritesActivity"
            android:label="Favorites"
            android:theme="@style/Theme.VideoEditorApp"/>
        <activity android:name=".UpgradeActivity"  android:theme="@style/Theme.VideoEditorApp"/>
        <activity android:name=".AppInfoActivity"  android:theme="@style/Theme.VideoEditorApp" android:label="App Info"/>
        <activity android:name=".SettingsActivity"
            android:label="Settings"
            android:theme="@style/Theme.VideoEditorApp"/>
        <activity android:name=".AssetStoreActivity"  android:theme="@style/Theme.VideoEditorApp" android:label="Creative HUB"/>
        <activity android:name=".AssetDetailActivity"  android:theme="@style/Theme.VideoEditorApp" android:label="Asset Preview"/>
        <activity
            android:name=".TimelineTemplateEditorActivity"
            android:screenOrientation="fullSensor"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize"
            android:label="Pro Editor(Beta)"
            android:theme="@style/Theme.VideoEditorApp"/>
        <activity android:name=".StorageDetailActivity" android:theme="@style/Theme.VideoEditorApp" android:label="Media Details"/>
        <activity android:name=".StorageActivity" android:theme="@style/Theme.VideoEditorApp" android:label="Storage Management"/>

        <!-- Authentication Flow -->
        <activity android:name=".LoginActivity" android:theme="@style/Theme.VideoEditorApp" android:exported="false" android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".RegisterActivity" android:theme="@style/Theme.VideoEditorApp" android:exported="false" android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".ProfileActivity" android:theme="@style/Theme.VideoEditorApp" android:label="Profile"/>
        <activity android:name=".OtpVerificationActivity" android:theme="@style/Theme.VideoEditorApp" android:exported="false" android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".ForgotPasswordActivity" android:theme="@style/Theme.VideoEditorApp" android:exported="false" android:windowSoftInputMode="adjustResize"/>
        <activity
            android:name=".ui.profile.CreatorProfileActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.VideoEditorApp.NoActionBar" />
    </application>

</manifest>